<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Dictionary" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:statusBarColor">@color/color_primary</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorSecondary">@color/color_secondary</item>
        <item name="colorAccent">@color/color_primary</item>
    </style>

    <style name="Theme.SplashScreenApp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:statusBarColor">@color/color_primary</item>
    </style>

    <style name="SplashScreenTheme" parent="Theme.SplashScreenApp">
        <item name="android:windowBackground">@color/color_primary</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/bg_splash_12</item>
        <item name="android:windowSplashScreenAnimationDuration">800</item>
    </style>

    <style name="ThemeOverlay.AppCompat.navTheme">

        <!-- Color of text and icon when SELECTED -->
        <item name="colorPrimary">@color/white</item>

        <!-- Background color when SELECTED -->
        <item name="colorControlHighlight">@color/color_primary_30</item>

    </style>

</resources>